const IS_MOBILE = 600;

const IS_TABLET = 768;

const IS_DESKTOP = 1024;

enum DeviceTypeEnum {
  mobil = "mobil",
  tablet = "tablet",
  desktop = "desktop",
}

export { DeviceTypeEnum, IS_MOBILE, IS_TABLET, IS_DESKTOP };
